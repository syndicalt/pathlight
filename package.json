{
  "name": "pathlight",
  "version": "0.1.0",
  "private": true,
  "description": "Visual debugging, execution traces, and observability for AI agents",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "license": "MIT",
  "dependencies": {
    "@pathlight/sdk": "^0.1.0"
  }
}
